{
  "buildCommand": "chmod +x node_modules/.bin/vite && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install && chmod +x node_modules/.bin/vite node_modules/.bin/esbuild",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
